workflows:
  android-apk:
    name: Android APK Build

    environment:
      java: 17

    scripts:
      - chmod +x gradlew
      - gradle assembleDebug

    artifacts:
      - app/build/outputs/**/*.apk
