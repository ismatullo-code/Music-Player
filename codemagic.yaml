workflows:
  android-build:
    name: Android APK Build

    environment:
      java: 17

    scripts:
      - chmod +x ./gradlew
      - ./gradlew assembleDebug

    artifacts:
      - app/build/outputs/**/*.apk
